name: Run Command Example

on:
  push:
    branches:
      - main

jobs:
  run_command:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run a command
        run: |
          echo "Hello, world!"
          echo "This is a command running in the workflow"
          # Add more commands here as needed
